# 0.0.6 候选发布筛查

## 2026-09-08 火绒拦截调查（发布阻塞）

- 主仓库现有改动已按快速预设、EA 启动、更新器、通知样式、构建依赖、
  发布文档分别提交。A/B 源码基线是 `d0bcfab`，不等同于已证明安全的“昨天版本”。
- 完整新版在原目录以 `cargo build --no-default-features --locked` 构建，
  火绒多次记录 `Trojan/Lakaboy`（`02B902CA0B023F8A`），由链接器生成文件时触发。
  包括 22:51:37、22:56:29 的重复记录。Cargo 返回成功不代表文件未被删除。
- 在同一原目录去掉 EA 原生启动组后未新增实时告警；但仅 EA、EA 加更新器、
  EA 加预设原生写入也未新增实时告警。最后一组的定向扫描在 22:55:19 完成：
  1 个文件、14 个对象、0 威胁。尚不能把完整组合的告警归因于某个单独函数。
- 两个工作目录共用的 `windows_tool` 本地依赖在对照期间保持原状；没有对它做
  回退实验。隔离工作目录的完整新版在清理应用调试缓存后重编译成功，
  未出现对应的实时告警（尚无该产物的定向扫描完成记录），因此目录、增量编译
  或二进制布局的影响尚待排除。所有实验均保留火绒防护。
- 定向扫描后的原目录文件拒绝删除，使第一次清理应用调试缓存未完成；不能将
  该次构建称作干净重编译。没有通过改名、改字符串或关防护来规避检测。
- 后续占用解除，原目录 `cargo clean -p mxtools --profile dev` 完成，完整源码
  重编译成功，但火绒在 23:03:40 再次记录同一病毒、同一 ID 的链接期拦截。
  因此旧增量缓存不是唯一原因。23:01:45 入库的旧产物定向扫描也确认了
  1 文件、1 威胁；不能把此前未记录扫描结果当作无威胁。当前新产物与隔离目录
  产物的定向扫描尚无完成记录，仍需在火绒处理完待处理结果后核实。
- `vite.config.ts` 已改用 `import.meta.dirname`；本轮类型检查及生产构建通过，
  原 `configLoader: native` 兼容性警告不再出现。此修复不代表木马告警解决。

> 2026-09-08 自动筛查更新：前端 105 个文件、526 项及 Rust 198 项通过，
> 7 项受控实机测试跳过；lint、类型检查、生产构建、Rust 格式与 clippy 通过。
> npm 全量及生产依赖审计均为零漏洞。快速预设新增 Steam/EA 的自定义 FPS、
> 全部画质档位、分辨率锁轴、等待启动器退出和缺失/空/部分 profile 回归覆盖。
> 更新安装前现会恢复后台硬件状态并保留 Tauri 退出清理。
>
> 当前仍不可公开发布：`E:/rust/windows_tool` 含未提交的 EA 路径修复，而 CI
> 固定旧提交 `5695e89d2a5e23475787c9df5ef5c58ba71e3205`；在线更新签名未配置；
> 下方人工冒烟未完成。本轮产物数据见下方 2026-09-08 表。

## 2026-09-08 本轮候选产物

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `萌新工具箱 0.0.6 便携版.exe` | 4,868,879 | `D07EA27DD6549F9D4C2FE4A256CB0EC367862942D0F3249F0A7FE232F3F4BE32` |
| `萌新工具箱 0.0.6 安装版.exe` | 4,894,736 | `497A8B8E77DF267A4F9FDCC8930D01E252D8351CD3BFAA04B5A2DDAAB63498FE` |
| `萌新工具箱 0.0.6 微软商店版.exe` | 217,271,632 | `526315DCB8E550A66C2508C275712C669A986C55BC1B109DCC936E61D7FAB63C` |

体积门禁已通过；这些文件来自含本地未提交 `windows_tool` 修复的工作区，
因此只作为候选验证，不应直接发布。

日期：2026-07-30

## 已完成

- [x] `package.json`、`.env`、`src-tauri/Cargo.toml` 和
  `src-tauri/tauri.conf.json` 版本统一为 `0.0.6`。
- [x] `package-lock.json` 与 `src-tauri/Cargo.lock` 已更新，并解除 Git 忽略。
- [x] `npm.cmd run lint` 通过。
- [x] 前端 28 个测试文件、107 项测试通过。
- [x] `cargo fmt --check` 通过。
- [x] `cargo clippy --all-targets -- -D warnings` 通过。
- [x] Rust 76 项测试通过、0 项失败、1 项管理员 SMB 集成测试忽略。
- [x] `npm.cmd run "build window release"` 成功。
- [x] `npm.cmd run release:size:check` 通过；便携版和安装版均严格小于
  5,000,000 字节。
- [x] 同一发布命令生成普通安装版、缓存式便携版和微软商店版共 3 份产物；
  微软商店版内置 WebView2 x64 离线安装器，不受 5 MB 门禁限制。
- [x] WebView2 离线安装器为微软官方有效签名文件（203,778,256 字节，
  SHA-256 `04B9F08D839C8C06F34A85ACEA0D9F1568D3D8AA309A77619AAA46BB29ADE0F8`），
  后续构建直接复用 `src-tauri/target/webview2/` 缓存。
- [x] 缓存式 NSIS 包装器探针连续启动两次均返回 0；首次缓存 SHA-256
  匹配内层 EXE，第二次缓存文件时间戳未变化，确认没有重复解压。
- [x] 发布便携版已完成一次真实启动并创建主窗口；测试进程已停止，临时
  Beta 设置已用测试前备份恢复，`betaFeaturesEnabled` 未留在用户配置中。

## 发布产物

| 产物 | 字节数 | SHA-256 |
| --- | ---: | --- |
| `萌新工具箱 0.0.6 便携版.exe` | 4,917,338 | `AD3F3B9DD958BF01B0E24E16D23857CA89497F5AE6522786C35D04643615BF46` |
| `萌新工具箱 0.0.6 安装版.exe` | 4,941,688 | `984C6EE360EAD81A3BA72626ABC9ABD3E393A5EF4AF86B7BCD05B1D6C719C7B1` |
| `萌新工具箱 0.0.6 微软商店版.exe` | 211,360,089 | `5609E08D0A195C104152C465F216013E64E4C513CBBF044E042BA48834A02F1B` |

产物目录：`src-tauri/target/release/0.0.6/`。

## 发布范围

本次 `0.0.6` 仅发布到 GitHub Releases，不提交微软商店。商店上架相关事项列在
「暂不适用」一节，不构成本次发布的阻塞项。内置 WebView2 离线安装器的那份产物
本次仅作为离线安装选项随 Release 提供。

## 发布阻塞项

- [ ] 重新构建三份产物：下方「发布产物」表的体积与 SHA-256 早于当前提交，需
  重跑 `npm.cmd run "build window release"` 与 `npm.cmd run release:size:check`
  后用新结果替换该表。
- [ ] 按 `docs/superpowers/manual-smoke-bugfix-sweep.md` 完成 9 项 Windows
  真机人工冒烟；本轮按维护者要求跳过界面冒烟，后续由实际使用反馈问题。
- [ ] 在实际 Apex/Steam、多显示器及混合 DPI 环境验证 APEX Q 截图、OCR
  下载/升级、引擎兜底和悬浮窗定位。
- [ ] Release 说明中标注产物未签名并附 SHA-256 校验值，提示 Windows/SmartScreen
  可能显示未知发布者；不得把未签名描述为受信任发布，也不得用自签名证书冒充
  公网可信签名。
- [x] 关闭当前正在运行的 debug 实例后，启动新的缓存式便携版，确认主窗口
  正常创建并使用 `%LOCALAPPDATA%\mxtools\portable-cache\0.0.6` 中的 EXE；
  二次启动复用缓存，未重复解压。
- [ ] 人工验收完成后再创建提交、`v0.0.6` 标签、推送和发布条目。

## 暂不适用（微软商店路线）

本次不走商店，以下条目保留供后续上架时使用。

- [ ] 零固定成本签名优先申请 SignPath Foundation 的开源项目计划；若资格或
  审核不通过，则继续保留未签名下载及 SHA-256 校验。另一条免费路线是未来改
  为 MSIX 并通过 Microsoft Store 认证后由商店重签；当前 EXE/NSIS 路线不会
  获得商店免费重签。
- [ ] 未打包 EXE 提交路线要求：用受信任 CA 的代码签名证书签署安装器及其中
  所有 PE 文件，将不可变的版本化安装器放到 HTTPS 下载地址，并在 Partner
  Center 配置 `/S` 静默安装参数。
- [ ] 准备 Partner Center 商店资料：至少一个语言的说明、1 张截图、商店图标、
  年龄分级、市场范围和认证备注，然后提交认证。
- [x] 根目录已有 `LICENSE`、`NOTICE` 和 `THIRD_PARTY_NOTICES.md`。MxTools 使用
  自定义非商业源码可用许可证，不应在商店资料中误称为 OSI 开源许可证；上架
  时按实际许可证和第三方声明填写。

自动化工具无法捕获当前无边框 Tauri 窗口（`0x80004002`）；本轮不以自动化
界面操作替代人工验收，也未自动修改真实 Steam、输入法、RDP、防火墙或端口
转发状态。当前机器仅有一块 `2560x1440` 显示器，未运行 Steam/PUBG，
因此多显示器、混合 DPI、真实游戏配置、OCR 下载/升级和游戏联动仍未覆盖。
三份应用产物的 Authenticode 状态仍为 `NotSigned`；仅内置 WebView2 离线
安装器为微软有效签名（203,778,256 字节，SHA-256
`04B9F08D839C8C06F34A85ACEA0D9F1568D3D8AA309A77619AAA46BB29ADE0F8`）。
