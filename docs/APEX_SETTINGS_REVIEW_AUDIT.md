# Apex 待确认设置

尚无完整菜单映射的参数保持只读，显示真实文件值和兼容性说明。
新出现的未知键继续显示，同名键按文件区分，不跨文件套用说明。

武器对话使用单键 0/1。聊天连接功能使用 0/1/2/3，分别为关闭、提示文字、
语音、文字和语音。PS5 初始化标记不提供独立编辑，普通设置修改保留它。

其余 18 项仍保持只读。工具不凭键名推定用途，不把历史映射当成当前菜单。
`cl_safearea` 和 `hudchat_visibility` 的新写入限制为 0/1；
已有非规范值及未知值在读取、修改其他设置时保留。

覆盖测试位于 `tests/src/utils/game/apex_game_settings_review.test.ts` 和
`tests/rust/src-tauri/game/apex_review_settings.rs`，包括默认模板分类、
跨文件同名键、新未知键、说明搜索、非法写入拒绝及两次磁盘读取后原值保留。
