export enum ThemeEnum {
  system = 'Follow system',
  light = 'Light',
  dark = 'Dark',
}


export enum ApexFilterEnum {
  all = 'ALL',
  normal = 'NORMAL', // 常用的,不显示在Apex中可以设置的项
}

/** Apex 页面主标签：启动项 / 画面配置 */
export enum ApexPageTypeEnum {
  launch = 'launch',
  video_config = 'video_config',
}
